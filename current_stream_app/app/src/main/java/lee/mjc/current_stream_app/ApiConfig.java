package lee.mjc.current_stream_app;

// API 베이스 URL 설정 (값은 app/build.gradle의 API_BASE_URL에서 관리)
public final class ApiConfig {

    public static final String BASE_URL = BuildConfig.API_BASE_URL;

    private ApiConfig() {
    }
}
