spring:
  application:
    name: current-stream-backend
  datasource:
    url: jdbc:mysql://localhost:3306/currentstream?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: cs_user
    password: dik1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
